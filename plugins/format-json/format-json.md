Find all JSON files in the current directory and its subdirectories, then format them with proper 2-space indentation.

For each file:
1. Read the file
2. Parse and validate the JSON
3. Format it with 2-space indentation
4. Write it back to the file

Show a summary of how many files were formatted and report any errors encountered.
